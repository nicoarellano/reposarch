import * as FRAGS from "@thatopen/fragments";
import * as BUI from "@thatopen/ui";
import * as OBC from "@thatopen/components";
type Attributes = string | ((name: string) => boolean);
type AttributesToInclude = Attributes[] | ((defaultAttributes: Attributes[]) => Attributes[]);
export interface EntityAttributesUIState {
    components: OBC.Components;
    fragmentIdMap: FRAGS.FragmentIdMap;
    tableDefinition: BUI.TableDataTransform;
    editable?: boolean;
    attributesToInclude?: AttributesToInclude;
}
export declare const entityAttributesTemplate: (state: EntityAttributesUIState) => BUI.TemplateResult<1>;
export {};
